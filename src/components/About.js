import { Link } from "react-router-dom"

const About = () => {
    return (
        <div>
            <h4>Version 1.0.0 React App</h4>
            <Link to="/">Go back Home Page</Link>
        </div>
    )
}

export default About
